image: ruby:2.4

pages:
    tags:
        - docker
    script:
        - bundle install
        - gem install therubyracer
        - bundle exec middleman build --clean
    artifacts:
        paths:
            - public
    only:
        - master
